import React from 'react';
import SectionTitle from "../SectionTitle";

const RemoteWorkTitle: React.FC = () => {
    return (
        <SectionTitle title="العمل عن بعد" center={true} />
    );
};

export default RemoteWorkTitle;